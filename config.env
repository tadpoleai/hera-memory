# 任务配置(2026-07-12 确认)
BASENAME=20260712212422_fred_calib
MOUNTING_RPY="[0.0, 180.0, 0.0]"      # LiDAR 倒装(用户确认), ZYX 度
ROOM_COUNT=3                           # 用户确认:这次是3个房间的正式数据(此前 20260712175715_fred_calib 用户说明只是标定数据)

# --- 已废弃/不再适用的旧字段(见 STATUS.md 阶段 -1 决策记录) ---
# OSS_INPUT_PREFIX / OSS_OUTPUT_PREFIX: 不再需要。GLIM 建图改为本地
#   Docker 算子流水线(hera-desktop),不走 OSS 触发的云工作流。
# GPU_ECS_HOST: 不再需要。阶段 4 GPU 推理走阿里云函数计算(FC GPU)+
#   ACR 自定义镜像,参考 /home/fred/Code/hera-sdk-python/tools/fc-gpu
#   的部署模式(手动 docker build/push + FC 控制台配置,无 SSH ECS)。

# OSS 仍用于:FC GPU 阶段4 的输入/输出中转(见 secrets/aliyun.env 的
# OSS_BUCKET/OSS_ENDPOINT),桶名 uploaded-hera。

# --- 外部工具/代码位置(替代 CLAUDE.md 原设想的 tools/ 二进制) ---
HERA_SDK_PYTHON=/home/fred/Code/hera-sdk-python   # hera Python API(HeraFile 等)
HERA_DESKTOP=/home/fred/Code/mvp2/hera-desktop     # 本地 Docker 算子 + hera-run CLI
HERA_RUN_BIN=/home/fred/Code/mvp2/hera-desktop/target/debug/hera-run
FC_GPU_REF=/home/fred/Code/hera-sdk-python/tools/fc-gpu  # FC GPU 部署参考(风格,非直接复用)
